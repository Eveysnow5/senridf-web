# Cloudflare Pages 安全响应头（对所有路径生效）。
# 说明：
# - X-Content-Type-Options: 禁止浏览器猜测 MIME，防 MIME 混淆攻击
# - X-Frame-Options: 禁止被其它站点用 iframe 嵌入（防点击劫持）
# - Referrer-Policy: 跨站跳转时不泄露完整 URL
# - Permissions-Policy: 关闭定位/摄像头；麦克风仅允许本站（翻译语音需要）
# - Strict-Transport-Security: 强制 HTTPS 一年（本站 Cloudflare 全 HTTPS）
# - Content-Security-Policy-Report-Only: CSP 观察期版本（只报告、不拦截，漏配也不会白屏）
#
# 【CSP 说明】站点零构建 + 大量内联 <script>/style=，静态 _headers 无法注入 per-request nonce，
#   所以 script-src/style-src 只能用 'unsafe-inline'（牺牲了防内联 XSS 的能力）；但对 connect-src、
#   script-src 的“外部主机”做了严格白名单——即使被注入 XSS，也无法把数据外传到攻击者域名、无法加载
#   攻击者脚本。这是本站 CSP 的主要价值。
# 【当前是 Report-Only】违规只会打印到浏览器 DevTools 控制台（本站未接集中收集端点），不拦截任何请求。
#   观察期做法：镜像修好上线后，逐个打开各工具页（翻译/分析/校对/blog/admin 等），F12 看 Console 有无
#   CSP 违规；确认无遗漏来源后，把下面这行的 header 名从 Content-Security-Policy-Report-Only 改成
#   Content-Security-Policy 即可转为强制拦截。
# 【白名单来源出处】fonts.googleapis/gstatic=Google字体；www.gstatic=Firebase SDK；*.googleapis.com
#   +securetoken.google.com=Firebase后端；api.deepgram.com(+wss)=语音；sdf-admin.*.workers.dev=管理后台
#   Worker；cdn.jsdelivr/cdnjs.cloudflare=marked/dompurify/pdf.js/mammoth/xlsx(均带SRI)；cloudflareinsights=CF分析；
#   api.github.com=admin 发布后轮询镜像仓库 HEAD 确认上线（admin/index.html、admin/blog/index.html）。
# 【2026-08-24 追加】apis.google.com（script-src）与 senridfauthentication.firebaseapp.com
#   （frame-src）都是 **Firebase Auth 的弹窗/跳转登录解析器**：SDK 会加载 gapi 的
#   iframe 装载器，并在 authDomain 上开一个隐藏 iframe。本站只用邮箱密码登录，
#   **这套机制其实用不上**，放行的理由不是怕它坏，而是不放行的话后台错误日志里
#   会一直堆这三条已知的良性违规 —— 日志里堆着良性噪声，正是真违规被漏掉的路径。
#   来源：不是静态扫描发现的（这些 URL 由 SDK 在运行时拼出，带随机后缀如
#   ?onload=__iframefcb292202，静态扫描原理上看不到），是 report-error.js 的
#   securitypolicyviolation 上报捞到的。两条渠道互补的一个实证。
# 【frame-src】此前完全没声明，靠 default-src 'self' 回落 —— 能用但意图不可见。
#   现在显式写出。站内两个 iframe（admin 的页面预览、宏观看板）都是同源，不受影响。
/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: SAMEORIGIN
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), camera=(), microphone=(self)
  Strict-Transport-Security: max-age=31536000; includeSubDomains
  Content-Security-Policy-Report-Only: default-src 'self'; script-src 'self' 'unsafe-inline' 'wasm-unsafe-eval' https://www.gstatic.com https://apis.google.com https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://static.cloudflareinsights.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' data: https://fonts.gstatic.com; img-src 'self' data: blob: https:; connect-src 'self' https://*.googleapis.com https://securetoken.google.com https://api.deepgram.com wss://api.deepgram.com https://sdf-admin.sherlockafa.workers.dev https://api.github.com https://static.cloudflareinsights.com https://cloudflareinsights.com; worker-src 'self' blob: https://cdnjs.cloudflare.com; frame-src 'self' https://senridfauthentication.firebaseapp.com; frame-ancestors 'self'; base-uri 'self'; form-action 'self'; object-src 'none'
