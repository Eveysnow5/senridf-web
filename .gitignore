# Dependencies
node_modules/

# Environment variables
.env
.env.local
.env*.local

# Netlify
.netlify/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Claude Code 个人本地设置（不共享给同事）
.claude/settings.local.json

# Local tool config (contains API keys)
tools/document-analyzer/config.json

# 内部文档整体分离到私有仓库 sherlockafa007/senridoufuu-docs（2026-08-09）
# 原因：plans/specs 里逐条写着哪些弱点还没修，TOOLS.md 里有管理员邮箱和 Worker
# 地址，这些不该公开。本目录在磁盘上仍是 docs/，只是归属另一个（私有）仓库，
# 读写路径不变。提交文档用项目根目录的 docs-sync.sh。
docs/

# 历史游离文档（勿再提交）
FINAL_VERIFICATION.md
